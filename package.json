{
  "name": "roar-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "python3 -m http.server 5174",
    "build": "mkdir -p dist && cp -R index.html styles.css script.js CNAME assets dist/"
  }
}
